---
paths:
  - "projects/**"
---

# 코드 스타일

## 줄 길이

코드 생성 시 한 줄이 **120자를 넘지 않도록** 줄바꿈하라.

## 지역변수 추출

- 메서드 호출이 2단계 이상 중첩되면 지역변수로 추출하라. `Result.from(service.method(param.toCommand()))` 같은 중첩 인라인은 금지.
- 반대로, 한 번만 쓰이는 단순한 값을 굳이 변수로 빼지 마라.
